extends Node

# Entity Signals
signal onEntityClicked(entity)
signal onEntitySelected(entity)
signal onEntityMouseEntered(entity)
signal onEntityMouseExited(entity)

# Terrain Signals
signal onTerrainClicked(position)

